export enum AppRoute {
  Login = '/login',
  Details = '/parking/',
  Root = '/'
}

export enum AuthorizationStatus {
  Auth = 'AUTH',
  NoAuth = 'NO_AUTH',
  Unknown = 'UNKNOWN',
}

export const URL_MARKER_DEFAULT = '../img/pin.svg';
export const URL_MARKER_CURRENT = '../img/pin-active.svg';

export const TIMEOUT_SHOW_ERROR = 100;

export enum APIRoute {
  Main = '/hotels',
  Room = '/hotels/',
  Near = '/hotels/',
  Login = '/login',
  Logout = '/logout',
  Review = '/comments/',
  ParkingList = '/comments/'
}

